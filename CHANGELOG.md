<div dir="rtl">

# יומן שינויים

כל שינוי שמשפיע על מה שהמתקשר שומע, על מה שהספק מקבל, או על מה
שמופיע ביומן. הפורמט: הגרסה, ואז מה השתנה ולמה — הסיבה היא מה
שלא ניתן לשחזר מהקוד בעוד חצי שנה.

מה שנבדק בשיחת טלפון חיה מסומן **[נבדק בקו]**. מה שעבר בדיקות
בלבד מסומן **[ממתין לקו]**, ואין להסיק ממנו שהתנהגות הספק אומתה.

---

## 1.2.0 — 28.8.2026

### ספק "מרכזייה עצמית" — Asterisk עצמאי, ספק רביעי

מרכזייה שרצה ברשת שלכם, בלי כתובת חיצונית, בלי דומיין, בלי רישום
מול ספק ענן. המוח — התפריט, הזיהוי, העוזר, ההתראות — נשאר ב-ha_ivr;
המרכזייה היא שכבת תעבורה דקה. הצד של ה-Asterisk (מתקין, סקריפטים,
דיאלפלן) במאגר הנלווה `asterisk-ha-pbx`.

- **תפריט** דרך ממסר HTTP: המרכזייה שולחת מצב, מקבלת טקסט ומקשים.
- **עוזר קולי דרך AudioSocket** — אודיו PCM גולמי על TCP, בלי קודק
  ובלי סבב Opus. הייצוג הנקי של המקור מגיע ל-STT. השידור מקוצב עם
  כרית 200ms נגד ג'יטר של לולאת האירועים. **[נבדק בקו]**
- **התראות** דרך webhook שרץ במרכזייה, במקום `rest_command`. **[נבדק בקו]**
- הכתובת המוצגת להעתקה היא הפנימית (דגל `PREFER_INTERNAL_URL`), כי
  המרכזייה פונה ל-HA ברשת ולא מהאינטרנט.

החוזה (בקשה/תשובה, פרוטוקול ה-AudioSocket) מתועד ב-`docs/pbx.md`.

### כפתור החלפת טוקן, לכל ספק

הטוקן הוא הסוד שמגן על נקודת הקצה. אם דלף — כפתור בהגדרות מייצר
טוקן חדש; הישן מפסיק לעבוד מיד, ויש לעדכן את הכתובת אצל הספק.

### שלוחת "התראות אחרונות" — חדשות-בלבד, ומחיקה אחרי שמיעה

עד כה השלוחה הקריאה בכל כניסה את כל ההתראות של המתקשר, כולל מה
שכבר שמע, והיומן לא התנקה. עכשיו היא מקריאה רק את **החדשות**, מסמנת
כנשמעו, ומוחקת התראה שכל נמעניה שמעו. התראה רב-נמענית יורדת רק
כשגם האחרון שמע.

---

## 1.1.0 — 24.8.2026

### ערוצי התראה לימות — SMS וצינתוק לצד שיחה קולית

**התראה קולית עולה יחידה; SMS וצינתוק עולים עשירית יחידה.** נמדד
מול המחירון ואומת בשליחה חיה. בכרטיס הנמען יש עכשיו בורר ערוץ:
שיחה קולית / SMS / צינתוק. הבורר מונחה-יכולת — מופיע רק אצל ספק
שמצהיר `NOTIFY_CHANNELS`, כלומר ימות; טכנוליין שולח בקול בלבד.
**[נבדק בקו]**

### שלוחת "התראות אחרונות", והתאמה לכל נמען

צינתוק זול (עשירית יחידה) הופך למערכת התראות מלאה: פינג יוצא,
המתקשר חוזר, ושומע מה קרה — בשיחה נכנסת ולא בשיחה יוצאת יקרה.

- **כל התראה נרשמת ליומן**, בכל ערוץ, עם מספר הנמען.
- **שלוחת "התראות אחרונות"** — סוג תת-רשומה חדש, מקריאה את היומן.
- **התאמה לכל נמען:** כל מתקשר שומע רק את ההתראות שנשלחו אליו,
  לפי מספרו. התראה מיועדת לאדם מסוים, ושניים אינם מתערבבים.
- **הכרזה בשורש:** מתקשר עם התראה שטרם שמע — "יש לך התראה חדשה"
  בכניסה לתפריט, פעם אחת עד שיאזין. **[נבדק בקו]**

### תוקן

- **`api_wait_answer_music_on_hold` הוסר מההוראות בימות.** נמדד
  חי: היא רק מוסיפה הודעת המתנה גנרית של הספק, ואינה נחוצה —
  ההנחה הקודמת שבלעדיה יש שקט מביך הופרכה.

---

## 1.0.1 — 23.8.2026

- **הסמל של האינטגרציה הוחלף.** בית מלא בכחול-פלדה עם שפופרת
  בתוכו. הקודם השתמש בצללית הבית של Home Assistant ובכחול המדויק
  שלהם, מה שנקרא כאילו זו אינטגרציה רשמית — וסותר את הצהרת
  האחריות. הסמל החדש נקרא היטב גם ב-48 פיקסלים, הגודל שבו הממשק
  מציג אותו בפועל.

  התמונות נשלחות מתוך `custom_components/ha_ivr/brand/` וגוברות על
  ה-CDN של Home Assistant, כפי שנקבע ב-2026.3. אין צורך בהגשה
  למאגר `brands`.

- **בדיקת הסמלים בשער** קיבעה את רוחב הלוגו, ולכן נכשלה על כל
  עיצוב בפרופורציות אחרות. היא בודקת עכשיו את הדרישה עצמה: אייקון
  ריבועי במידה קבועה, לוגו בגובה קבוע לרוחב, ו-`@2x` שהוא הכפלה
  מדויקת.

---

## 1.0.0 — 23.8.2026

הגרסה הפומבית הראשונה.

### מה יש בה

- **תפריט DTMF** מול ימות המשיח, טכנוליין ו-Vonage. העץ נבנה
  מתת-רשומות בממשק, ומשוחזר מחדש בכל שיחה. **[נבדק בקו]**
- **עוזר קולי** בטכנוליין וב-Vonage, כישות `assist_satellite` מול
  צינור Assist — שיחה חופשית, קטיעה באמצע דיבור, וביטוי יציאה
  שמחזיר לתפריט. שני קווים במקביל לכל רשומה. **[נבדק בקו]**
- **התראות קוליות** בימות ובטכנוליין. כל נמען הוא ישות `notify`,
  ובטכנוליין ההקראה נוצרת ב-Home Assistant ולא אצל הספק.
  **[נבדק בקו]**

### מה שאומת בשיחות אמיתיות

- מקש שאינו מוגדר מגיע ונענה בקול, ולא נבלע בשקט
- מסנן התווים של ימות, שנמדד תו-תו: תו אחד מתוך 28 שובר את מנוע
  ההקראה שלהם
- שתי התראות לשני נמענים, שנחתו על שני קווים במקביל
- 16 קילוהרץ מקצה לקצה ב-Vonage, בלי המרת קצב

### מה שלא אומת

- **זיהוי ניתוק לפי היעדר אודיו** מעולם לא נורה — בכל השיחות עד
  היום המרכזייה סגרה את הסוקט ראשונה
- **שדות 7–10 ב-`read` של ימות** — המיפוי אינו מאושר, והקוד
  במכוון אינו מנחש אותו
- **VAD שלעיתים מפספס תחילת דיבור** — רמת הקלט נמדדה ונשללה
  כסיבה; ההשערה שנותרה מתועדת, לא מתוקנת
- **מנוע ההקראה של טכנוליין** נכשל בחלק מהחשבונות. ההתראות עוקפות
  אותו לגמרי

### מגבלות מצד הספקים

- לימות אין ערוץ סטרימינג, ולכן אין לה עוזר קולי
- ל-Vonage אין מסלול שיחות יוצאות, ולכן אין לה ישויות נמען

</div>
